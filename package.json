{
  "name": "wild-rydes-ride-fleet",
  "description": "",
  "version": "0.1.0",
  "dependencies": {
    "serverless-plugin-thundra-lambda-adapters-cw": "^1.0.2"
  },
  "devDependencies": {
    "serverless-python-requirements": "^3.3.1"
  },
  "scripts": {
    "setup": "pyenv virtualenv 3.6 $(basename $(pwd)) && pyenv local $(basename $(pwd)) && pip install -r requirements.txt -r requirements-dev.txt"
  }
}
